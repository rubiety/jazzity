%section#recent-members
  %h3 Recent Members:
  %ul
    - Musician.members.recent.each do |member|
      %li
        = link_to image_tag(member.avatar_url, :alt => "#{member}'s Avatar"), member, :class => "avatar", :title => member.to_s
